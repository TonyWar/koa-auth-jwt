import debug from 'debug';
const d = debug('example:2');

d('test');

// const path = require('path');
// const serve = require('koa-static');

// const publicFiles = serve(path.join(__dirname, 'public'));
// publicFiles._name = 'static /public';

// app.use(publicFiles);